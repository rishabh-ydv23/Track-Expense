# MongoDB Configuration
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/trackexpense

# JWT Configuration
JWT_SECRET=your-very-secure-jwt-secret-key-min-32-chars

# Server Configuration
PORT=4000
NODE_ENV=development

# CORS Configuration
CORS_ORIGIN=http://localhost:5173

# Set to 1 when running behind a reverse proxy (for rate limiting / IP)
# TRUST_PROXY=1
